## Evidence Snapshot (Read-Only)

The following queries were used during investigation
to confirm the presence of invalid ERP states.

These queries are documented here for transparency only.
Operational detection queries live in `detection/`.

Example observations:
- Assets with multiple active assignments detected
- Approved timesheets without approval records detected

